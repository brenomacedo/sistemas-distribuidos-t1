syntax = "proto3";

package sistemas_distribuidos;

enum MessageType {
  UNKNOWN = 0;
  DEVICE_DISCOVERY = 1;
}

enum ParamType {
  UNKNOWN2 = 0;
  DEVICE_IP = 2;
}

message MessageParams {
  optional ParamType type = 1;
  optional int64 value = 2;
}

message Message {
  optional MessageType type = 1;
  repeated MessageParams params = 2;
}
